spring:
  application:
    name: membership

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      javax:
        persistence:
          validations:
            mode: none

  datasource:
    url: ${DB_HOST}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  kafka:
    bootstrap-servers: ${KAFKA_HOST}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

server:
  port: 8085

userProfileClientUrl: ${USER_PROFILE_CLIENT_URL}
organizationClientUrl: ${ORGANIZATION_CLIENT_URL}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_HOST}